services:
  - type: web
    name: shalom-web
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run db:push && npm start
    envVars:
      - key: DATABASE_URL
        # Value will be requested by Render during Blueprint setup
      - key: NODE_ENV
        value: production

